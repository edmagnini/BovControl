export default class Farmer {
    constructor(
        id,
        farmer,
        production,
        date
    ) {
        this.id = id]
        this.farmer = farmer
    }
    getId() {
        return id
    }
}