export default class Farmer {
    constructor(
        id,
        name,
        farmer
    ) {
        this.id = id
        this.name = name
        this.farmer = farmer
    }
    getId() {
        return id
    }
}