export default class Farmer {
    constructor(
        id,
        name,
        email,
        password,
    ) {
        this.id = id
        this.name = name
        this.email = email
        this.password = password
    }
    getId() {
        return id
    }
}